clear
clc

beta = [1 2 3 4 5];


modelfun  = @(beta,x)(beta(1)*x(:,2)-x(:,3)/beta(5))./(1+beta(2)*x(:,1)+beta(3)*x(:,2)+beta(4)*x(:,3));

x = 10*rand(4,100);
y = modelfun(beta,x);  


 
beta0 = [1 1 1 1 1];
beta_estimate = nlinfit(x(:,1:4),y,@myfitfun,beta0);
temp = 1;